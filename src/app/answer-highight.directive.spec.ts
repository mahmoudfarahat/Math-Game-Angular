import { AnswerHighightDirective } from './answer-highight.directive';

describe('AnswerHighightDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerHighightDirective();
    expect(directive).toBeTruthy();
  });
});
